# gd-maintenance — 2026-09-25 — Pass B (freshness & content)

**Weekday:** Friday → Pass B per MAINTENANCE.md.
**Outcome: RED.** The database is unreachable from this run's environment. Everything in
Pass B that depends on `psql "$SUPABASE_DB_URL"` — which is nearly all of it — could not
run. Per MAINTENANCE.md §1 / the task prompt: an unreachable DB is itself the finding, not
a reason to skip the pass silently. Reporting and pushing per that rule.

## 0. DB reachability

| Check | Result |
|---|---|
| `SUPABASE_DB_URL` present in env | yes |
| DNS for `aws-1-eu-west-1.pooler.supabase.com` | resolves (3 A records via the ELB) |
| `psql "$SUPABASE_DB_URL" -c 'select 1'` | hangs, no error, times out |
| Raw TCP to the host on port 5432 | hangs (no RST, no data) — consistent with the sandbox's network policy silently dropping non-HTTP(S) egress rather than the DB itself being down |

**This looks like an environment network-policy gap, not a Supabase outage**: this cloud
session's outbound network only has an allowed path for HTTP(S) (through the pre-configured
agent proxy); a raw Postgres TCP connection on 5432 has no such path and is dropped rather
than refused. `./scripts/db-health.sh` and `./scripts/verify-push-eligible.sh` (Pass A, not
required today) would fail the same way for the same reason.

**Action needed (human):** widen this environment's network access (or add the pooler host
to its allowed domains) so `gd-maintenance` can reach Postgres on future runs. Until then,
every Friday/Tuesday run will report the same RED for the DB-dependent sections below.

## 1–8. Everything requiring DB (stale-data-audit skill §1–7, MAINTENANCE.md Pass B items 2–3)

**BLOCKED — not run.** This covers: which clubs are in the league, managers vs
`teams.manager_name`, squad/player photo freshness, team-page card staleness, player
dossiers, season-state phase vs calendar, and the relegated-clubs-still-getting-content
check. None of these could be checked this run. No automatic-source SQL fixes were made
(there was nothing to read from, and no before-value to log) and no human-source proposals
could be evaluated (manager staleness, photo-placeholder checksums) because both require a
DB round trip.

Rule 1 (source-of-truth) and Rule 2 (never loop a paid API call) were not exercised this
run since no fix was possible either way — recorded here for completeness, not because
either was tested.

## 9. Store copy and marketing — checked (no DB needed)

- `APP_STORE_V2.0_COPY.md` (the copy actually pasted into App Store Connect for the current
  submission, per its own header) states "all 20 Premier League clubs supported" and the
  "no spam / not at 3am" claims correctly. **Green.**
- `APP_STORE_STRATEGY.md` still says "Choose his team: Arsenal, Manchester United, or West
  Ham" (a 3-team pitch) and frames the app as "$10, no free trial." This is the V1.0
  (2026-02-08) strategy doc, explicitly superseded by `APP_STORE_V2.0_COPY.md` — it is
  historical, not live copy, so no action taken. Flagging here only so a future audit
  doesn't re-discover it as if it were new.
- Price/free-vs-paid in **code**: grepped `ios/` for price copy — no hardcoded price string
  found (the in-app paywall code was deleted 2026-09-23 per a comment in
  `GoalDiggerApp.swift`; the app has no in-app price copy to go stale). App Store Connect's
  actual live listing price was not checked — outside this repo's reach.

## 10. iOS constants — checked (no DB needed)

- `grep -n "hideAfter\|WCSeason" ios/GoalDigger/Models/FeedContext.swift` → no matches.
  Consistent with §11 below (WC retirement already landed): there is no live World
  Championship date-gate left to check for drift.

## 11. World Cup retirement (MAINTENANCE.md Pass B item 5) — self-revision needed

MAINTENANCE.md's Pass B item 5 says "World Cup surfaces are being retired (branch
`claude/retire-world-championship`)". That branch **no longer exists on origin** (fetch
fails with "couldn't find remote ref"), and its work is already merged:

- `d1e43e9` "Retire the tournament feed rather than keep an off switch that no longer works
  (#17)", 2026-09-24, is on `origin/claude/feature-planning-1q4m3p` (this repo's default
  branch) already.
- Per that commit's message: all 48 country rows and the `world_championship` entity are
  `is_active = false`, `poll_leagues()` no longer returns league 1, the client-side date
  gate (`WCSeason.hideAfter`) is gone (confirmed above), and the group/best-third maths is
  kept in `_shared/` for a future tournament.

**This retirement already happened — it is not a pending Friday task any more.** See §12
for the MAINTENANCE.md edit this produced.

## Auto-fixes made this run

None. Every candidate fix in Pass B needs the DB.

## Pushes sent

One — see below. Reason: DB unreachable (per MAINTENANCE.md §4, this alone requires a push).

## Report written

This file. Committed together with the MAINTENANCE.md self-revision (§12).

## 12. Self-revision applied to MAINTENANCE.md

- Pass B item 5 rewritten: the WC retirement (PR #17, 2026-09-24) has landed, the branch is
  gone, so this is no longer a live watch item. Replaced with a note to treat any WC surface
  found stale in future as leftover-to-delete, not a retirement-in-progress.

No change made to `DATA_SOURCES.md` this run (the deep audit that would surface a new feed
drift did not run, so there is nothing new to record there).
