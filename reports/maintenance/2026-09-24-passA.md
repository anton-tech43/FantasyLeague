# Maintenance report — 2026-09-24 — Pass A (drift & health)

Run type: manual/off-schedule (today is Thursday; MAINTENANCE.md/prompt route any
non-Tue/Fri day to Pass A). Routine: `gd-maintenance`. Branch: `claude/gallant-mendel-gbg957`.

## Headline finding: DB/backend unreachable — root cause is this session's network policy, not Supabase

`SUPABASE_DB_URL` was present in the environment (injected as expected), but every
attempt to reach Supabase failed:

- `./scripts/db-health.sh`: PostgREST checks (feed, team page, squad, season state)
  all timed out after 12s ("hung, not refused"); the Postgres pooler probe failed on
  3/3 attempts. Verdict line: "whole project unreachable".
- Direct `psql "$SUPABASE_DB_URL" -c 'select 1'` against
  `aws-1-eu-west-1.pooler.supabase.com:5432` timed out (no response) after 20s.
- Raw TCP to that host:5432 failed outright (`/dev/tcp` connect failed).
- The agent egress proxy's own status log shows repeated `403` "policy denial" on
  CONNECT to a `rest:443` host in the same window — i.e. this session's outbound
  network policy is denying the Supabase hosts, not Supabase itself refusing the
  connection (a refusal or a real outage would show a fast TCP RST/HTTP error, not
  a 12–20s hang plus a proxy-side 403).

**This is being reported as the finding per the two hard rules** ("If the DB is
UNREACHABLE ... THAT is the finding: report it and push. Do not skip the pass
silently"). It is very likely an environment configuration gap (Supabase's REST/DB
hosts not on this session's network allowlist) rather than a real production
incident — but that distinction needs a human to confirm from outside this sandbox,
so it is being pushed rather than assumed.

**Everything below that depends on DB access (A2–A6) could not be checked this run.**
No auto-fixes were attempted — there was nothing reachable to fix, and this class of
failure (network policy, not data) is out of scope for an autonomous SQL fix anyway.

## Checklist

| # | Check | Result |
|---|-------|--------|
| A1 | Backend serving (`db-health.sh`) | 🔴 RED — see above. PostgREST + pooler both unreachable. |
| A2 | pg_cron failures (`cron.job_run_details`, 72h) | ⬜ NOT RUN — blocked, no DB access. |
| A3 | pg_net bloat (`net._http_response`) | ⬜ NOT RUN — blocked, no DB access. No auto-fix attempted. |
| A4 | Routines producing (`content_items` per source) | ⬜ NOT RUN — blocked, no DB access. |
| A5 | `./scripts/verify-push-eligible.sh` | ⬜ NOT RUN — script itself requires `backend/.env` (gitignored, absent by design in this cloud checkout per MAINTENANCE.md §1) and a local `/opt/homebrew` psql path; even fixed, it needs the same DB connection that is unreachable this run. |
| A6 | API balance / IDLE_TIMEOUT sanity | ⬜ NOT RUN — no Edge Function invocation was made (nothing to check logs against without DB/Supabase access); no cost incurred, Rule 2 not implicated. |
| A7 | Secret hygiene | 🟢 GREEN — `./scripts/pre-commit-secret-scan.sh` exit 0, no matches. `.claude/worktrees/` does not exist in this checkout (glob found nothing) — no stray `.env` copies. |

## Auto-fixes applied

None. Nothing in A2/A3 (the only auto-fixable items) could be evaluated without DB
access, and A7 required no fix.

## Push sent

Yes — via `PushNotification`, one line, per the "DB was unreachable" trigger in
MAINTENANCE.md §4. Content: backend unreachable, likely a network-policy gap in this
session rather than Supabase, see this report.

## Self-revision (§5)

No drift found in `MAINTENANCE.md` or `DATA_SOURCES.md` this run — the checklist
itself matched what Pass A attempted to do; it was the environment, not the doc,
that was out of date. No changes made to either file.

## Follow-up for next run / for a human

- Confirm whether this session's environment network policy allows
  `*.supabase.co` / `*.pooler.supabase.com` — if not, add them (Environment settings
  → Network access) so `gd-maintenance` can actually reach the DB on future runs.
- Until that's fixed, every future Pass A/B run will hit the same wall and should
  report the same way rather than silently no-op.
