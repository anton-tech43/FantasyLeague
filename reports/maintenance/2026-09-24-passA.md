# gd-maintenance — Pass A (drift & health) — 2026-09-24

Run type: scheduled, no human watching. UTC weekday = Thursday -> treated as
Pass A per the routine's "any other day -> Pass A" rule.

## 0. Pre-flight: canonical docs

Looked for `MAINTENANCE.md`, `DATA_SOURCES.md`, `BACKFILL_RULES.md`, `CLAUDE.md`
at repo root, and for the `scripts/` directory (`db-health.sh`,
`verify-push-eligible.sh`, `pre-commit-secret-scan.sh`) and a `reports/`
directory. **None of these exist** — not on the checked-out branch
(`claude/create-markdown-file-hIdbj`), not on `origin/main`, and not on any
of the other ~18 remote branches in this repo. This is not drift (a stale
value in an existing file); the scaffolding this scheduled routine's prompt
assumes has never been committed to this repository at all.

Status: **RED** (blocked). Ran the rest of Pass A from the routine's own
embedded fallback instructions (the prompt says to follow those if
`MAINTENANCE.md` is missing), substituting manual checks where the missing
scripts made the prescribed command impossible.

## 1. DB health / connectivity

- `$SUPABASE_DB_URL` is **not set** in the environment. Only `SUPABASE_URL`
  and `SUPABASE_SERVICE_KEY` (a Supabase service-role API key, not a
  Postgres connection string) are present.
- `psql "$SUPABASE_DB_URL" -c "select 1;"` fails immediately: with the
  variable empty, psql falls back to a local Unix socket
  (`/var/run/postgresql/.s.PGSQL.5432`) which doesn't exist in this
  container.
- Fallback probe: `GET $SUPABASE_URL/functions/v1/health-check` with the
  service key as bearer/apikey — timed out after 20s with 0 bytes received
  (outbound network policy for this session does not appear to reach
  `*.supabase.co`; the agent proxy itself reported no relay failures,
  consistent with the destination not being on the allowed egress list
  rather than a Supabase-side outage).

**Per the routine's Rule/DB-access instructions: this is the finding.**
Status: **RED — DB unreachable (missing `SUPABASE_DB_URL` secret + no
network path to Supabase from this session)**. No SQL-based checks below
(pg_cron job history, pg_net bloat, squad/injury freshness, etc.) could be
run this pass. Nothing was skipped silently — each is logged as blocked
below rather than assumed healthy.

## 2. pg_cron job failures (last 72h)

Blocked — requires DB access (see §1). Not checked.

## 3. pg_net (`net._http_response`) bloat / TRUNCATE

Blocked — requires DB access. **No write was attempted.** (Even if reachable,
this is exactly the kind of destructive-looking write the routine reserves
for a confirmed-safe, well-understood condition; skipping it while DB
access is down is the conservative choice, not a missed auto-fix.)

## 4. Routine content freshness (gd-news / gd-insider / gd-season-state /
gd-quiz / gd-matchday cadence)

Blocked — requires DB access to read `content_items`. Not checked.

## 5. `verify-push-eligible.sh`

Script does not exist anywhere in the repo (see §0). Not run. Treated as
**blocked**, not as a pass.

## 6. team-page-generator / IDLE_TIMEOUT (API balance)

Not invoked this run (no team-page-generator call was made — nothing in
this pass required LLM regeneration, and Rule 2 forbids looping it across
teams regardless). No IDLE_TIMEOUT signal was checked because that would
also require either DB or function access, both blocked per §1.

## 7. Secret scan / stray worktrees

`pre-commit-secret-scan.sh` doesn't exist, so this was done manually as a
substitute (read-only, no risk):
- `find .claude -iname worktrees` → none found; no stray
  `.claude/worktrees/*/.env` files.
- `git grep` across tracked files for common secret shapes
  (Anthropic `sk-ant-…` keys, Supabase `sb_secret_…` keys, AWS access
  key IDs, PEM private key headers) → **no matches**.
- No `.env` files are tracked in git anywhere in the working tree.

Status: **GREEN** (manual substitute check only — not the same coverage
as the real script, which does not exist to compare against).

## 8. Repository / branch sanity

- Current branch `claude/create-markdown-file-hIdbj` is well ahead of and
  diverged from `origin/main` (many merge commits from
  `claude/intelligent-thompson`); this looks like an iOS/content feature
  branch, not a dedicated maintenance branch. No changes were made to
  application code this run.
- `origin/claude/retire-world-championship` exists as the prompt expects
  (World Cup surfaces retirement branch), but Pass B (not this pass) is
  where WC content would be checked — not evaluated today.

## Auto-fixes applied this run

**None.** DB was unreachable, so no automatic-source SQL fixes (squad
surname matching, injury dedupe, null backfills, pg_net truncation) could
be attempted. No human-source proposals were generated for the same
reason — nothing could be read to compare against DATA_SOURCES.md, which
also doesn't exist.

## Pushes sent

One `PushNotification` sent this run: DB unreachable (missing
`SUPABASE_DB_URL`) **and** the entire maintenance scaffolding
(`MAINTENANCE.md`, `DATA_SOURCES.md`, `BACKFILL_RULES.md`, `scripts/`,
prior `reports/`) is absent from the repository on every branch checked.
Both are explicit push conditions in the routine's own rules ("DB was
unreachable"; effectively every check this pass depends on is blocked).

## Self-revision (last step)

Did **not** author new `MAINTENANCE.md` / `DATA_SOURCES.md` /
`BACKFILL_RULES.md` / `CLAUDE.md` files or a `scripts/` directory from
scratch this run. The routine's last step is to *self-revise* those files
"if they drifted from the app" — that presumes they already exist and
need updating for accuracy, not that a full policy/tooling framework
should be invented unilaterally by an unattended run with no way to
verify DB state against whatever rules it wrote. Fabricating the
source-of-truth map or the safety scripts while the DB itself is
unreachable, with no human to sanity-check the result, is exactly the
kind of judgment call this routine should push rather than guess at.
Only this `reports/maintenance/` directory and this report were added.
